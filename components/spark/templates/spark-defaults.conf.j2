# Auto-generated by DemoForge
spark.master                     local[*]
spark.driver.memory              512m
spark.executor.memory            512m

# S3A Configuration for MinIO
{%- for edge in edges %}
{%- if edge.target == node.id and edge.connection_type == 's3' %}
{%- set source_node = nodes | selectattr('id', 'equalto', edge.source) | first %}
{%- if source_node %}
spark.hadoop.fs.s3a.endpoint         http://{{ project_name }}-{{ source_node.id }}:9000
spark.hadoop.fs.s3a.access.key       minioadmin
spark.hadoop.fs.s3a.secret.key       minioadmin
spark.hadoop.fs.s3a.path.style.access true
spark.hadoop.fs.s3a.impl             org.apache.hadoop.fs.s3a.S3AFileSystem
spark.hadoop.fs.s3a.connection.ssl.enabled false
{%- endif %}
{%- endif %}
{%- endfor %}

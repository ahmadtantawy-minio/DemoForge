# Auto-generated by DemoForge
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

{% for edge in edges %}
{% if edge.target == node.id and edge.connection_type == 'metrics' %}
{% set source_node = nodes | selectattr('id', 'equalto', edge.source) | first %}
{% if source_node %}
  - job_name: '{{ source_node.id }}'
{% if source_node.component == 'minio' %}
    metrics_path: '/minio/v2/metrics/cluster'
    static_configs:
      - targets: ['{{ project_name }}-{{ source_node.id }}:9000']
{% else %}
    static_configs:
      - targets: ['{{ project_name }}-{{ source_node.id }}:9090']
{% endif %}
        labels:
          instance: '{{ source_node.id }}'
          component: '{{ source_node.component }}'
{% endif %}
{% endif %}
{% endfor %}

/** Defaults when enabling AIStor Tables on a MinIO node or MinIO cluster (matches lakehouse Spark + sovereign edge naming). */
export const AISTOR_TABLES_DEFAULT_ICEBERG_WAREHOUSE = "warehouse";
/** Default MinIO/cluster ``AISTOR_TABLES_CATALOG_NAME`` when enabling AIStor Tables (not stored on MinIO→Trino edges). */
export const AISTOR_TABLES_DEFAULT_CATALOG_NAME = "aistor";
